import './bootstrap';

// Initialization for ES Users
import {
    Animate,
    Carousel,
    Collapse,
    Dropdown,

    initTE,
  } from "tw-elements";

initTE({ Animate, Carousel, Collapse, Dropdown,});